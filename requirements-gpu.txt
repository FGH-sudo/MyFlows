# GPU 训练加速（CuPy，需与本机 CUDA 版本匹配）
# 当前完整 Toolkit 为 CUDA 13.x：
cupy-cuda12x>=14.0.1

# ONNX 推理 / eval / serve_grpc / 仿真 pilot
# pip install onnxruntime-gpu
# 还需 CUDA 12 + cuDNN 9 的 DLL；已安装 PyTorch+cu128 时，device_runtime 会自动把 torch\lib 加入 PATH
